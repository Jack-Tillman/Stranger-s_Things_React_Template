import React, {useState, useEffect} from "react";


const Home = ({editComplete, setEditComplete, isLoggedIn, userAccount}) => {
setEditComplete(false);
return (
    <div className="home-screen-container">hello</div>
)
}


export default Home;